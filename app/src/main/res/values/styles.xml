<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:fontFamily">@font/font_lydiasama</item>

        <item name="android:windowContentTransitions">true</item>

        <item name="android:textViewStyle">@style/MyTextViewStyle</item>
    </style>

    <style name="MyTextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/font_lydiasama</item>
    </style>

    <style name="AppTheme.TextAppearance">
        <item name="android:fontFamily">@font/font_lydiasama</item>
    </style>

    <style name="FontProfile" parent="AppTheme.TextAppearance">
        <item name="fontFamily">@font/lydia1_thick</item>
        <item name="android:textSize">@dimen/font_header</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:singleLine">true</item>
    </style>

</resources>
